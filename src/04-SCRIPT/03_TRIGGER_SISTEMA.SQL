CREATE TABLE LOGIN_AUDIT(
LOGIN_USER_NAME VARCHAR(30),
LOGIN_USER_DATA DATE
);

CREATE OR REPLACE TRIGGER LOGIN_AUDIT_TRIGGER
AFTER LOGON ON DATABASE

BEGIN
INSERT INTO LOGIN_AUDIT VALUES (
ORA_LOGIN_USER,
SYSDATE);
END;
/ 